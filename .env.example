# DeepSeek (OpenAI-compatible). Server-only — never prefix with NEXT_PUBLIC_.
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=

# Supabase Postgres (Prisma). Server-only.
# DATABASE_URL — pooler / transaction mode for the app
# DIRECT_URL — direct connection for migrations
DATABASE_URL=
DIRECT_URL=
